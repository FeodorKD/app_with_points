import React, {FC} from 'react';
import classes from './MainPage.module.css'
const MainPage: FC = () => {
    return (
        <div className={classes.title}>
            <h1>It is a test task</h1>
        </div>
    );
};

export default MainPage;